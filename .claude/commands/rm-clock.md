---
description: Start real-time digital clock via Virtual Stylus on reMarkable 2 screen
---

Run the virtual stylus digital clock:
```bash
rm-ai clock $ARGUMENTS
```
Display live clock status, delta updates, and positioning options.
