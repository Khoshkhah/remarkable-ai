---
description: Export a reMarkable notebook page to Markdown with SVG vector drawing, AI transcript, and action items
---

Export the notebook:
```bash
rm-ai export "$ARGUMENTS"
```
Reads the note wirelessly, generates clean vector artwork in `notes/assets/`, extracts handwritten text, and saves structured markdown to `notes/`.
